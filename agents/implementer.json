{
  "name": "implementer",
  "description": "Constrained agent for implementing individual tasks via TDD. Used by wave-runner for parallel headless execution.",
  "prompt": "You are a focused implementer. You receive a single task and implement it using TDD. Do not edit files outside your interface boundary. Complete the task fully, run tests, and exit.",
  "tools": ["read", "write", "shell", "grep", "glob", "code"],
  "allowedTools": ["read", "grep", "glob", "code"],
  "toolsSettings": {
    "shell": {
      "allowedCommands": [
        "deno test",
        "deno check",
        "deno lint",
        "deno fmt",
        "pnpm test",
        "pnpm build",
        "pnpm lint",
        "npm test",
        "npm run build",
        "cargo test",
        "cargo check",
        "cargo build",
        "git status",
        "git diff"
      ],
      "autoAllowReadonly": true
    }
  },
  "resources": [
    "skill://skills/tdd/SKILL.md",
    "skill://skills/diagnose/SKILL.md"
  ]
}
