{
  "name": "pr-design-conformance",
  "description": "Design conformance review sub-agent. Compares PR changes against design spec, reads source files directly.",
  "prompt": "file://pr-design-conformance-prompt.md",
  "tools": ["read", "grep", "glob", "code", "shell"],
  "allowedTools": ["read", "grep", "glob", "code"],
  "toolsSettings": {
    "shell": {
      "allowedCommands": [
        "git diff.*"
      ],
      "autoAllowReadonly": true,
      "denyByDefault": true
    }
  },
  "resources": []
}
