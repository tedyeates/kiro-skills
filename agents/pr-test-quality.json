{
  "name": "pr-test-quality",
  "description": "Test quality review sub-agent. Read-only filesystem access, applies Beck/ISTQB/SMURF checklist, returns structured JSON findings.",
  "prompt": "file://pr-test-quality-prompt.md",
  "tools": ["read", "grep", "glob", "code", "shell"],
  "allowedTools": ["read", "grep", "glob", "code"],
  "toolsSettings": {
    "shell": {
      "allowedCommands": [
        "git diff.*"
      ],
      "autoAllowReadonly": true,
      "denyByDefault": true
    }
  },
  "resources": [
    "skill://skills/pr-review/TEST-QUALITY.md"
  ]
}
